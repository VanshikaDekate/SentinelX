from fastapi import FastAPIcc
from dotenv import load_dotenv
from app.routers.weather_router import router as weather_router

# Load .env so OPENWEATHER_API_KEY is available
load_dotenv()

app = FastAPI(title="SentinelX")


@app.get("/")
def read_root():
    return {"message": "SentinelX API running"}


# Include your weather router
app.include_router(weather_router)
